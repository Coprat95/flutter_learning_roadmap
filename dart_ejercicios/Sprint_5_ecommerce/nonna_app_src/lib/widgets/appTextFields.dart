import 'package:flutter/material.dart';

class Apptextfields {
  static const TextField textFieldApp = TextField(                  
                      decoration: InputDecoration(hintText: 'mail_example@mail.com',
                      prefixIcon:Icon(Icons.mail)
                      ),
                    );
}